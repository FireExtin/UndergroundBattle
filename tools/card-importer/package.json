{
  "name": "@undergroundbattle/card-importer",
  "private": true,
  "version": "0.1.0",
  "description": "Purpose: Normalizes organized card, rule, and token resources into stable JSON datasets for the Go authority and TypeScript clients.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest --run",
    "test:watch": "vitest",
    "import:data": "npm run build && node ./dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}

