{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://undergroundbattle.local/shared/protocol/messages.schema.json",
  "title": "ProtocolMessageEnvelope",
  "description": "Defines the minimal message envelope shared between the web client and the Go authority.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "kind", "messageId", "name", "payload"],
  "properties": {
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Semver version for the transport schema."
    },
    "kind": {
      "type": "string",
      "enum": ["command", "event", "view"],
      "description": "Top-level channel discriminator."
    },
    "messageId": {
      "type": "string",
      "minLength": 1,
      "description": "Correlation identifier generated by the sender."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Concrete message name inside the chosen channel."
    },
    "revision": {
      "type": "integer",
      "minimum": 0,
      "description": "Optional authoritative state revision."
    },
    "payload": {
      "type": "object",
      "description": "JSON payload carried by the envelope.",
      "additionalProperties": true
    }
  }
}

