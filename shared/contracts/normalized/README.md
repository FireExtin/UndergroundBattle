# shared/contracts/normalized

Purpose: stores normalized JSON generated from the shared CardLogic DSL fixtures for downstream tooling and review.

The normalized contract output includes both runtime-facing logic fields and minimal source anchors such as `cardName` and `sourcePath`.
