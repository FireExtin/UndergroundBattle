# shared/contracts/expectations

Purpose: reserved for future golden outputs and richer expectation assets.

The current minimal CardLogic DSL fixtures store their expectations inline inside each fixture JSON.
