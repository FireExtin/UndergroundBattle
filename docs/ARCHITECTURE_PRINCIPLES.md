# 架构原则

## 最高优先级约束

1. 这是游戏，不要求现在或未来的向后兼容性。
2. 一切以架构整洁、抽象得当、便于理解为最高宗旨。
3. 过渡层、兼容层、双轨语义如果不再服务当前真相源，应直接删除，而不是长期保留。
4. Go 规则核是唯一真相源；前端只消费投影和元数据，不持有业务裁决。

## 当前工程解释

- “不向后兼容”既包括协议字段，也包括内部状态结构、过渡 helper、旧测试夹具和文档口径。
- 如果一个抽象同时表达“当前真实实现”和“未来可能实现”，优先只保留当前真实实现；等未来规则准备好，再做一次清晰的替换。
- 临时实现可以存在，但不能伪装成长期并行架构。
